#include <bits/stdc++.h>
#define rep(i, n) for (int i = 0; i < (n); ++i)
using namespace std;
typedef long long ll;
const int INF = 1 << 30;
const ll MOD = 1000000007;

float route(int x, int y) {}

int main() {
    int N;
    cin >> N;
    int x[N], y[N];
    rep(i, N) { cin >> x[i] >> y[i]; }

    return 0;
}