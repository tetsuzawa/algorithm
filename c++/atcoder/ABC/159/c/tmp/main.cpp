#include <bits/stdc++.h>
#define rep(i, n) for (int i = 0; i < (n); ++i)
using namespace std;
typedef long long ll;
const int INF = 1 << 30;
const ll MOD = 1000000007;

int main() {
    int l;
    cin >> l;
    double v;
    v = (double)l / 3 * (double)l / 3 * (double)l / 3;
    cout << (float)v << endl;

    return 0;
}