#include <bits/stdc++.h>
#define rep(i, n) for (int i = 0; i < (n); ++i)
using namespace std;
typedef long long ll;
const int INF = 1 << 30;
const ll MOD = 1000000007;

int sum()

    int main() {
    int n, m;
    cin >> n >> m;
    int a[n - 1];
    rep(i, n) cin >> a[i];
    return 0;
}